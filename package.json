{
  "name": "merni-edinici",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:seo": "node build.mjs",
    "build:all": "vite build && node build.mjs",
    "preview:local": "node build.mjs && vite preview --port 5173"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "vite": "^5.4.0"
  }
}
